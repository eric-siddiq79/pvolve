{% if block.settings.image != blank %}
    <div class="sub-menu__col">
        <div class="sub-menu-card">
            <div class="sub-menu-card__figure">
                {{ block.settings.image | image_url: width: 312, height: 388 |  image_tag }}
            </div>
            <div class="sub-menu-card__content">
                <h3 class="sub-menu-card__title">{{ block.settings.title }}</h3>
                <a href="{{ block.settings.url }}" class="sub-menu-card__link">{{ block.settings.link }}</a>
            </div>
        </div>
    </div>    
{% endif %}


{% schema %}
{
  "name": "Menu Card",
  "tag": null,
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Label"
    },
    {
      "type": "url",
      "id": "url",
      "label": "Link"
    }
  ],
  "presets": [
    {
      "name": "Menu Card"
    }
  ]
}
{% endschema %}
